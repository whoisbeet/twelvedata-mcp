import type { VercelRequest, VercelResponse } from '@vercel/node';
import { callTool, toolDefs } from './core.js';
const serverInfo = { name: 'twelvedata-mcp', version: '4.0.0' };
const ok=(id:unknown,result:unknown)=>({jsonrpc:'2.0',id,result}); const failure=(id:unknown,code:number,message:string)=>({jsonrpc:'2.0',id:id??null,error:{code,message}});
async function handle(q:any, req: VercelRequest) { if(!q||q.jsonrpc!=='2.0'||typeof q.method!=='string') return failure(q?.id,-32600,'Invalid Request'); if(q.method==='initialize') return ok(q.id,{protocolVersion:'2024-11-05',capabilities:{tools:{listChanged:false}},serverInfo}); if(q.method==='notifications/initialized') return null; if(q.method==='ping') return ok(q.id,{}); if(q.method==='tools/list') return ok(q.id,{tools:toolDefs}); if(q.method==='tools/call'){try{return ok(q.id,{content:[{type:'text',text:JSON.stringify(await callTool(String(q.params?.name),q.params?.arguments||{},{env:process.env,headers:req.headers as any,query:req.query as any}),null,2)}]})}catch(e){return ok(q.id,{isError:true,content:[{type:'text',text:e instanceof Error?e.message:'Tool failed'}]})}} return failure(q.id,-32601,`Method not found: ${q.method}`);}
export default async function handler(req:VercelRequest,res:VercelResponse){res.setHeader('Access-Control-Allow-Origin','*');res.setHeader('Access-Control-Allow-Methods','GET,POST,OPTIONS');res.setHeader('Access-Control-Allow-Headers','Content-Type,Accept,MCP-Protocol-Version,Last-Event-ID');if(req.method==='OPTIONS')return res.status(204).end();if(req.method==='GET'){res.setHeader('Content-Type','text/event-stream');res.setHeader('Cache-Control','no-cache, no-transform');return res.status(200).send('event: endpoint\\ndata: /mcp\\n\\n')}if(req.method!=='POST')return res.status(405).json({error:'MCP endpoint accepts GET SSE or POST JSON-RPC.'});try{const body=typeof req.body==='string'?JSON.parse(req.body):req.body;const result=Array.isArray(body)?(await Promise.all(body.map(x=>handle(x,req)))).filter(Boolean):await handle(body,req);return res.status(200).json(result)}catch(e){return res.status(400).json(failure(null,-32700,e instanceof Error?e.message:'Parse error'))}}
