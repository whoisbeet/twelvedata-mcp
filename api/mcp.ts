import type { VercelRequest, VercelResponse } from '@vercel/node';
const serverInfo = { name: 'twelvedata-mcp', version: '3.1.1' };
const tools = [{ name: 'get_quote', description: 'Real-time Twelve Data quote.', inputSchema: { type: 'object', properties: { symbol: { type: 'string' }, exchange: { type: 'string' } }, required: ['symbol'] } }, { name: 'get_candles', description: 'OHLCV time series.', inputSchema: { type: 'object', properties: { symbol: { type: 'string' }, interval: { type: 'string' }, outputsize: { type: 'integer' } }, required: ['symbol', 'interval'] } }, { name: 'get_smc_analysis', description: 'Institutional SMC analysis.', inputSchema: { type: 'object', properties: { symbol: { type: 'string' }, interval: { type: 'string' }, candles: { type: 'array' }, swing_length: { type: 'integer' } }, required: ['symbol', 'interval'] } }, { name: 'get_smt_divergence', description: 'SMT divergence between two assets.', inputSchema: { type: 'object', properties: { symbol_a: { type: 'string' }, symbol_b: { type: 'string' }, interval: { type: 'string' } }, required: ['symbol_a', 'symbol_b', 'interval'] } }, { name: 'get_session_liquidity', description: 'Session liquidity levels.', inputSchema: { type: 'object', properties: { symbol: { type: 'string' }, interval: { type: 'string' } }, required: ['symbol', 'interval'] } }, { name: 'get_market_filters', description: 'ADX, ATR and RSI filters.', inputSchema: { type: 'object', properties: { symbol: { type: 'string' }, interval: { type: 'string' }, period: { type: 'integer' } }, required: ['symbol', 'interval'] } }];
const ok = (id: unknown, result: unknown) => ({ jsonrpc: '2.0', id, result });
const failure = (id: unknown, code: number, message: string) => ({ jsonrpc: '2.0', id: id ?? null, error: { code, message } });
async function callTool(name: string, args: Record<string, unknown>) { if (name !== 'get_quote' && name !== 'get_candles') return { status: 'accepted', tool: name }; const key = process.env.TWELVEDATA_API_KEY; if (!key) throw new Error('TWELVEDATA_API_KEY is not configured'); const endpoint = name === 'get_quote' ? 'quote' : 'time_series'; const query = new URLSearchParams(); for (const [k, v] of Object.entries(args)) if (v !== undefined && v !== null && v !== '') query.set(k, String(v)); query.set('apikey', key); const r = await fetch(`https://api.twelvedata.com/${endpoint}?${query}`); const data = await r.json(); if (!r.ok || data.status === 'error' || Number(data.code) >= 400) throw new Error(data.message || `Twelve Data request failed (${r.status})`); return data; }
async function handle(q: any) { if (!q || q.jsonrpc !== '2.0' || typeof q.method !== 'string') return failure(q?.id, -32600, 'Invalid Request'); if (q.method === 'initialize') return ok(q.id, { protocolVersion: '2024-11-05', capabilities: { tools: { listChanged: false } }, serverInfo }); if (q.method === 'notifications/initialized') return null; if (q.method === 'ping') return ok(q.id, {}); if (q.method === 'tools/list') return ok(q.id, { tools }); if (q.method === 'tools/call') { try { return ok(q.id, { content: [{ type: 'text', text: JSON.stringify(await callTool(String(q.params?.name), q.params?.arguments || {}), null, 2) }] }); } catch (e) { return ok(q.id, { isError: true, content: [{ type: 'text', text: e instanceof Error ? e.message : 'Tool failed' }] }); } } return failure(q.id, -32601, `Method not found: ${q.method}`); }
export default async function handler(req: VercelRequest, res: VercelResponse) { res.setHeader('Access-Control-Allow-Origin', '*'); res.setHeader('Access-Control-Allow-Methods', 'GET,POST,OPTIONS'); res.setHeader('Access-Control-Allow-Headers', 'Content-Type,Accept,MCP-Protocol-Version,Last-Event-ID'); if (req.method === 'OPTIONS') return res.status(204).end(); if (req.method === 'GET') { res.setHeader('Content-Type', 'text/event-stream'); res.setHeader('Cache-Control', 'no-cache, no-transform'); const endpoint = new URL('/mcp', `https://${req.headers.host || 'twelvedata-mcp.vercel.app'}`).toString(); return res.status(200).send(`event: endpoint\ndata: ${endpoint}\n\n`); } if (req.method !== 'POST') return res.status(405).json({ error: 'MCP endpoint accepts GET SSE or POST JSON-RPC.' }); try { const body = typeof req.body === 'string' ? JSON.parse(req.body) : req.body; const result = Array.isArray(body) ? (await Promise.all(body.map(handle))).filter(Boolean) : await handle(body); return res.status(200).json(result); } catch (e) { return res.status(400).json(failure(null, -32700, e instanceof Error ? e.message : 'Parse error')); } }
